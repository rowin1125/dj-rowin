{
  "name": "dj-rowin",
  "version": "1.0.0",
  "description": "Build in jekyll",
  "main": "index.html",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "deploy": "rm -rf docs && jekyll build && touch docs/CNAME && echo 'https://djrowin.nl' > docs/CNAME && git add . && git commit -m 'Deploy' && git push"
  },
  "repository": {
    "type": "https://github.com/rowin1125/dj-rowin",
    "url": ""
  },
  "author": "Rowin mol",
  "license": "ISC",
  "dependencies": {}
}
